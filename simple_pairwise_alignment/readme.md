# This Python script reads an aligned DNA FASTA file and prints:
- Each sequence without its FASTA header.
- A line between each sequence pair showing matches (`|`) and mismatches (space).

This assignment is from the 6th week of class
```
alignment_visual.py <FASTA file>
